# Construct CNN samples list to use with tune_abund_cnn and fit_abund_cnn

Construct CNN samples list to use with tune_abund_cnn and fit_abund_cnn

## Usage

``` r
get_partition_samples(
  data,
  x,
  y,
  response,
  folds,
  partition,
  rasters,
  crop_size
)
```

## Value

a list of arrays
